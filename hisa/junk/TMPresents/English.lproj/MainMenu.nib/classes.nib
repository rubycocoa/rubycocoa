{
    IBClasses = (
        {
            ACTIONS = {
                goToFirst = id; 
                goToLast = id; 
                goToNext = id; 
                goToPrev = id; 
                reloadDocument = id; 
                startFullScreen = id; 
                stopFullScreen = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {showFontPanel = id; }; 
            CLASS = TMAppDelegate; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}