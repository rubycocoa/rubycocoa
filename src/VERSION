VERSION = "0.5.0"
STAGE = ""
RELEASE_DATE = "2006-12-07"
SVN_REVISION = proc {
	         vers = `svnversion`
                 rev = $?.success? ? vers.strip : 'unknown'
		 if rev == 'exported' 
		   ma = /\$Revision:\s*(\d+)\s*\$/.match("$Revision$")
		   rev = ma ? ma[1] : 'unknown'
		 end
               }.call
