VERSION = "1.0.2"
STAGE = ""
RELEASE_DATE = "2011-09-02"
SVN_REVISION = proc {
                 vers = `svnversion`
                 rev = $?.success? ? vers.strip : 'unknown'
                 if rev == 'exported' 
                   ma = /\$Revision:\s*(\d+)\s*\$/.match("$Revision$")
                   rev = ma ? ma[1] : 'unknown'
                 end
                 rev
               }.call
